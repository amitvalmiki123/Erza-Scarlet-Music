if [ -n "$COOKIES" ]; then
    echo "$COOKIES" > cookies.txt
    echo "COOKIES LOADED SUCCESSFULLY FROM ENV!"
    head -n 2 cookies.txt
else
    echo "WARNING: COOKIES ENVIRONMENT VARIABLE IS EMPTY!"
fi

uv run pip install -U yt-dlp
python3 server.py &
uv run python3 -m anony